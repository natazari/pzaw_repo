<form action="/collections/<%= collection_id %>/artists" method="POST">
  <div>
    <label for="artist_name">Nazwa artysty</label>
    <input
      type="text"
      name="artist_name"
      id="artist_name"
      required
      minlength="3"
      maxlength="100"
      <% if (locals.name){ %>
      value="<%= name %>"
      <% }%>
    />
  </div>
  <div>
    <button>Dodaj</button>
  </div>
</form>
